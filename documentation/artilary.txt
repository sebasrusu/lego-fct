cd /home/ydigit/NOVAFCT/cloud/lego-fct

# remover link/dir antigo e criar symlink para a pasta de imagens que indicaste
[ -e images ] && rm -rf images
ln -s /home/ydigit/NOVAFCT/cloud/proj-test/images images

# garantir package.json e instalar dependências locais (sem sudo)
[ -f package.json ] || npm init -y
npm install --no-audit --no-fund --save-dev artillery @faker-js/faker

# correr o load
npx artillery run load-data.yml